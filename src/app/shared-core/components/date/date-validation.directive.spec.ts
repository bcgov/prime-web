import { DateValidationDirective } from './date-validation.directive';

describe('DateValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DateValidationDirective();
    expect(directive).toBeTruthy();
  });
});
