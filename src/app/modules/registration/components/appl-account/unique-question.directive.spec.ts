import { UniqueQuestionDirective } from './unique-question.directive';

describe('UniqueQuestionDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueQuestionDirective();
    expect(directive).toBeTruthy();
  });
});
