import { OnInit, EventEmitter, ViewContainerRef } from '@angular/core';
import { ModalDirective } from 'ngx-bootstrap';
import { CommonImage } from '../../../images/src/images';
export declare class ThumbnailComponent implements OnInit {
    imageObject: CommonImage;
    reviewMode: boolean;
    deleteImage: EventEmitter<CommonImage>;
    fullSizeViewModal: ModalDirective;
    private viewContainerRef;
    constructor(viewContainerRef: ViewContainerRef);
    scaledWidth: number;
    ngOnInit(): void;
    delete(evt: any): void;
    showFullSizeView(): void;
    hideFullSizeView(): void;
}
