import { OnInit, EventEmitter } from '@angular/core';
export declare class FormActionBarComponent implements OnInit {
    submitLabel: string;
    canContinue: boolean;
    isLoading: boolean;
    defaultColor: boolean;
    btnClick: EventEmitter<any>;
    constructor();
    ngOnInit(): void;
    onClick($event: any): boolean;
}
