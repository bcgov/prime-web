import { Validator, FormControl } from '@angular/forms';
export declare class DayValidationDirective implements Validator {
    validate(control: FormControl): {
        [key: string]: any;
    } | null;
}
