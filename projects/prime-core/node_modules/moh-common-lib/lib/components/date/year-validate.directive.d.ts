import { Validator, FormControl } from '@angular/forms';
export declare class YearValidateDirective implements Validator {
    commonYearValidate: string;
    validate(control: FormControl): {
        [key: string]: any;
    } | null;
}
