import { OnInit, EventEmitter } from '@angular/core';
import { Base } from '../../../models/src/base';
export declare class ToggleComponent extends Base implements OnInit {
    data: boolean;
    label: string;
    dataChange: EventEmitter<boolean>;
    constructor();
    ngOnInit(): void;
}
