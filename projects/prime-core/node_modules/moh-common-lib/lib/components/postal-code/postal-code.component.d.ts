import { OnInit } from '@angular/core';
import { MaskModel } from '../../../models/src/mask.model';
export declare class PostalCodeComponent extends MaskModel implements OnInit {
    label: string;
    displayMask: boolean;
    maxlen: string;
    placeholder: string;
    pcFormat: RegExp;
    constructor();
    ngOnInit(): void;
}
